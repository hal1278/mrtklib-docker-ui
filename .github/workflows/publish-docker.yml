name: Publish Docker Image

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      mrtklib_branch:
        description: 'MRTKLIB branch or tag to build against (overrides .mrtklib-version)'
        required: false
        default: ''

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository_owner }}/mrtklib-docker-ui
            hatognss/mrtklib-docker-ui
          tags: |
            # Tag-push (release): semver flavour + sha- alias for git→image
            # traceability. {{major}}.{{minor}} / {{major}} / latest are
            # only emitted for stable tags (no '-' in the version) so alpha
            # releases don't displace the latest stable.
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}},enable=${{ github.ref_type == 'tag' && !contains(github.ref_name, '-') }}
            type=semver,pattern={{major}},enable=${{ github.ref_type == 'tag' && !contains(github.ref_name, '-') }}
            type=raw,value=latest,enable=${{ github.ref_type == 'tag' && !contains(github.ref_name, '-') }}
            type=sha,prefix=sha-,enable=${{ github.ref_type == 'tag' }}
            # workflow_dispatch (dev build): single overwrite-in-place tag
            # named after the branch, so repeated manual triggers do not
            # accumulate one sha-XXXXXXX image per click. Slashes in the
            # branch name are normalised by metadata-action.
            type=ref,event=branch,prefix=dev-,enable=${{ github.event_name == 'workflow_dispatch' }}

      - name: Determine build args
        id: args
        shell: bash
        run: |
          # UI app version: from release tag (strip leading 'v'),
          # or a PEP 440 dev version derived from the commit SHA.
          REF="${GITHUB_REF_NAME}"
          if [[ "$GITHUB_REF_TYPE" == "tag" && "$REF" =~ ^v ]]; then
            APP_VERSION="${REF#v}"
          else
            APP_VERSION="0.0.0.dev0+g${GITHUB_SHA::7}"
          fi
          echo "app_version=$APP_VERSION" >> "$GITHUB_OUTPUT"

          # MRTKLIB branch/tag: workflow_dispatch input > .mrtklib-version > main
          MRTKLIB_BRANCH="${{ inputs.mrtklib_branch }}"
          if [ -z "$MRTKLIB_BRANCH" ]; then
            if [ -f .mrtklib-version ]; then
              MRTKLIB_BRANCH="$(tr -d '[:space:]' < .mrtklib-version)"
            fi
          fi
          if [ -z "$MRTKLIB_BRANCH" ]; then
            MRTKLIB_BRANCH="main"
          fi
          echo "mrtklib_branch=$MRTKLIB_BRANCH" >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            APP_VERSION=${{ steps.args.outputs.app_version }}
            MRTKLIB_BRANCH=${{ steps.args.outputs.mrtklib_branch }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Image summary
        run: |
          echo "### Published images" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          echo "${{ steps.meta.outputs.tags }}" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
